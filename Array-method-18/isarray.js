//How to verify given value is array or not?
let product={}
let users=[]

console.log(Array.isArray(product))

if (Array.isArray(users)) {
    console.log("Array")
} else {
        console.log("Not Array")
}